import React from 'react';

import Ionicons from 'react-native-vector-icons/Ionicons';
import {Colors} from '../../style/index';

const size = 20;

const HomeIcon = () => {
  return <Ionicons name="home-outline" color={Colors.SECONDARY} size={size} />;
};

export default HomeIcon;
